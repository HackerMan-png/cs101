let intValue = prompt("What is the initial value?");
let ratio = prompt("What is the ratio?");
let n = prompt("What is n?");

let value = (intValue * (ratio ** (n - 1)));

console.log(`The value will be ${value}`);