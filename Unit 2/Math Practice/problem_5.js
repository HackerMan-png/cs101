let mass = prompt("What is the mass in grams");
let volume = prompt("What is the volume in meters cubed");

let density = (mass / volume);

console.log(`The density will be ${density} grams per meter cubed.`);